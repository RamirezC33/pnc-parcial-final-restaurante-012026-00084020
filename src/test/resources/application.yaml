spring:
  datasource:
    url: jdbc:h2:mem:restaurante_test;DB_CLOSE_DELAY=-1;MODE=PostgreSQL
    driver-class-name: org.h2.Driver
    username: sa
    password: ""
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt:
    secret: test-only-secret-not-used-in-production-min-32-bytes
    access-expiration-minutes: 15
    refresh-expiration-days: 7
  seed:
    admin-username: admin
    admin-password: Admin123!
